pipeline {
        stage('dev') {
            steps {
                echo 'development'
            }
        }
        stage('prod') {
            steps {
                echo 'production'
            }
        }
}
